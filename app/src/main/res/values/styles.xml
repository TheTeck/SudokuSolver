<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="InputButton" parent="android:Widget.Button" >
        <item name="android:background">@drawable/sudoku_button</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">16sp</item>
    </style>

    <style name="CreatePuzzleButton" parent="android:Widget.Button" >
        <item name="android:background">@drawable/sudoku_button</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:drawableLeft">@drawable/ic_extension_white_24dp</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="EraseCellButton" parent="android:Widget.Button" >
        <item name="android:background">@drawable/sudoku_button</item>
        <item name="android:paddingLeft">16dp</item>
        <item name="android:paddingRight">16dp</item>
        <item name="android:drawableLeft">@drawable/ic_check_box_outline_blank_white_24dp</item>
        <item name="android:drawablePadding">16dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textSize">16sp</item>
    </style>

</resources>
